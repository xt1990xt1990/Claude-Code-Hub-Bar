# CCH Bar v1.2.3

更新内容：

- 排行页的时间范围和排行对象切换改为滑动胶囊动效。
- 状态栏轮询支持空闲/活跃两组频率：空闲可选 1s / 3s / 5s，活跃可选 1s / 3s。
- 新增状态栏低功耗动效开关，可关闭运行光环、滚动文字和缓存呼吸灯，默认仍保留完整特效。
- 日志数组增加 500 条上限，减少长时间运行后的内存增长。
- 状态栏 snapshot 增加回归测试，避免仅因生成时间变化触发无意义更新。
- 清理仓库中已跟踪的历史 release zip，后续打包产物只用于 GitHub Release 上传，不再提交进仓库。
