# CCH Bar v1.2.6

更新内容：

- 渠道页新增名称搜索，只筛选当前选中的分组，渠道较多时可以更快定位目标。
- 完善搜索交互：点击搜索框外任意位置即可结束输入；聚焦时增加蓝色外圈光晕，并适配系统“减弱动态效果”设置。
- Sub2API 的 Cookie-only 登录态支持自动续期，并会保存服务端轮换后的 Cookie，减少登录态意外失效。
- Mac 从睡眠唤醒并恢复网络后，会自动刷新上游登录态、倍率和余额，避免继续使用过期状态。
- 优化上游 Key 库刷新链路：同一站点只需完成一次扫描、匹配和刷新，降低重复解析与请求开销，同时改善大量渠道下的响应速度。
