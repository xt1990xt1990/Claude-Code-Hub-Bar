# CCH Bar v1.2.1.1

更新内容：

- 修正渠道页供应商品牌图标识别：渠道卡片优先使用 CCH 返回的供应商类型 `providerType`，避免渠道名称中包含 `claude` 时误显示 Claude 图标。
- 日志和请求记录仍优先按实际模型名称识别图标，保持真实 Claude/OpenAI/Gemini/DeepSeek 请求展示准确。
- 调整总览内容区域的横向布局，修正最近请求紧凑行在方案 A 下的宽度对齐。
- 修正上游倍率自动同步调度：按保存的绝对运行时间补跑，睡眠/重启/解锁后不再错过到期同步；如果同一时刻已有刷新在跑，不会误记为已同步并推迟下一次。
